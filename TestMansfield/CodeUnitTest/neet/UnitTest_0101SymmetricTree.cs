using LeetCodeTony;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace leetcodeUnitTest;

[TestClass]
public class UnitTest_0101SymmetricTree
{
/*    [TestMethod]
    public void Test_1()
    { 

        TreeNode root = new TreeNode(
            1, 
            new TreeNode(2, 
                new TreeNode(3, null, null), 
                new TreeNode(4, null, null)
                ), 
            new TreeNode(2, 
                new TreeNode(4, null, null), 
                new TreeNode(3, null, null)
                )
        );

        var result = new _0101SymmetricTree().IsSymmetric(root);

        Assert.AreEqual(true, result);
    }

    [TestMethod]
    public void Test_2()
    { 

        TreeNode root = new TreeNode(
            1, 
            new TreeNode(2, 
                null, 
                new TreeNode(3, null, null)), 
            new TreeNode(2, 
                null, 
                new TreeNode(3, null, null)));

        var result = new _0101SymmetricTree().IsSymmetric(root);

        Assert.AreEqual(false, result);
    }   */ 
}